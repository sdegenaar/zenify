name: zenify
description: React Query for Flutter with smart async state management, automatic caching, hierarchical DI, and zero-boilerplate reactivity. Best of all worlds.

version: 1.2.1
homepage: https://github.com/sdegenaar/zenify
repository: https://github.com/sdegenaar/zenify
issue_tracker: https://github.com/sdegenaar/zenify/issues
documentation: https://github.com/sdegenaar/zenify/tree/main/doc

topics:
  - state-management
  - react-query
  - async-state
  - hierarchical-di
  - reactive

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0