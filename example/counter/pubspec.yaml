name: zenify_counter_example
description: Clean counter example demonstrating the Zenify V2 three-step pattern.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  zenify:
    path: ../../  # This references the main package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true